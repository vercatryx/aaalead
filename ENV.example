# Supabase Database Configuration
# Option 1: Use full DATABASE_URL (recommended)
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# Option 2: Use individual credentials (defaults to direct connection - faster)
# Project ref from your Supabase URL: https://[PROJECT-REF].supabase.co
SUPABASE_PROJECT_REF=hxsjkzatrfefeojvaitn
# Get your database password from: Supabase Dashboard → Settings → Database → Database Password
SUPABASE_DB_PASSWORD=your-database-password-here

# Connection type (optional):
# - Default: Direct connection (faster, IPv6 only) - RECOMMENDED
# - Set SUPABASE_USE_POOLER=true to use pooler (slower, IPv4 compatible)
# SUPABASE_USE_POOLER=false
# SUPABASE_POOLER_REGION=us-west-2  # Only needed if using pooler

# R2 Storage Configuration (Cloudflare)
NEXT_PUBLIC_R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
NEXT_PUBLIC_R2_ACCESS_KEY_ID=your-access-key-id
NEXT_PUBLIC_R2_SECRET_ACCESS_KEY=your-secret-access-key
NEXT_PUBLIC_R2_BUCKET_NAME=your-bucket-name
NEXT_PUBLIC_R2_PUBLIC_DOMAIN=https://pub-xxxxx.r2.dev

# Legacy React App variables (for backward compatibility)
# REACT_APP_R2_ENDPOINT=...
# REACT_APP_R2_ACCESS_KEY_ID=...
# REACT_APP_R2_SECRET_ACCESS_KEY=...
# REACT_APP_R2_BUCKET_NAME=...
# REACT_APP_R2_PUBLIC_DOMAIN=...
